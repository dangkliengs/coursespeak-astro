---
export const prerender = false;

export async function GET({ params }: any) {
  return {
    title: `Test page for ${params.slug}`
  };
}

const { title } = Astro.props;
---

<html>
  <body>
    <h1>{title}</h1>
  </body>
</html>